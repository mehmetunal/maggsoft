<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
	  <TargetFramework>$(TargetFramework)</TargetFramework>
	  <Copyright>$(Copyright)</Copyright>
	  <Company>$(Company)</Company>
	  <Authors>$(Company)</Authors>
	  <Version>0.1.2</Version>
	  <Description>The Maggsoft.Core project contains a set of core classes for maggsoft, such as caching, events, helpers, and business objects (for example, Order and Customer entities).</Description>
	  <PackageLicenseUrl>$(PackageLicenseUrl)</PackageLicenseUrl>
	  <PackageProjectUrl>$(PackageProjectUrl)</PackageProjectUrl>
	  <RepositoryUrl>$(RepositoryUrl)</RepositoryUrl>
	  <RepositoryType>$(RepositoryType)</RepositoryType>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Libraries\EventBus\Maggsoft.EventBus.IoC\Maggsoft.EventBus.IoC.csproj" />
    <ProjectReference Include="..\..\Libraries\EventBus\Maggsoft.EventBus\Maggsoft.EventBus.csproj" />
  </ItemGroup>

</Project>
